# 鼓励储蓄罐（Reward Jar）项目阶段性总结

> **项目版本**：v1.3
> **总结日期**：2026-06-07
> **项目状态**：已上线运行
> **部署地址**：`https://m.runbt.cn` / `https://mypoint.longw7050.workers.dev`

---

## 一、项目背景与目标回顾

### 1.1 项目起源

"鼓励储蓄罐"（Reward Jar）源于一个真实的家庭教育需求：家长希望用一种可量化、可追溯的方式来记录和奖励孩子的良好表现。传统的口头表扬容易流于形式，而直接给予物质奖励则缺乏仪式感和持续性。因此，需要一个轻量级的积分管理系统，将孩子的积极行为转化为可视化的积分，并通过积分兑换奖励的正向激励机制，培养孩子的良好习惯。

### 1.2 核心目标

| 目标 | 描述 |
|------|------|
| **行为量化** | 将"小红花""做家务""考试优秀"等行为转化为积分 |
| **正向激励** | 通过"攒积分→换奖励"建立延迟满足和努力回报的认知 |
| **家庭透明** | 家长和孩子都能查看积分记录，做到公平公正 |
| **简单易用** | 移动端优先，操作流程简洁，降低家长使用门槛 |
| **数据隐私** | 数据存储在浏览器本地，不经过任何服务器 |

### 1.3 核心业务规则

- **积分获取**：课堂获得1朵小红花 = 2积分（类别可自定义）
- **积分兑换**：1积分 = 1元人民币
- **数据存储**：浏览器 LocalStorage 本地持久化

---

## 二、需求理解与分析过程

### 2.1 需求分析

经过多轮需求澄清，确定了以下功能边界：

1. **孩子管理**：支持添加多个孩子，每个孩子有独立的积分账户
2. **积分记录**：记录每笔积分的获得与消费，支持按日期分组展示
3. **积分类别**：预设常见类别（小红花、做家务、考试成绩等），支持用户自定义
4. **奖励商城**：展示可兑换的奖励物品，支持积分消费
5. **移动端适配**：核心使用场景是手机端，需要良好的触屏操作体验
6. **数据持久化**：使用 LocalStorage 存储所有数据，无需后端服务

### 2.2 用户体验优化需求

在初始版本完成后，用户提出了移动端体验优化需求，主要包括：
- 底部导航栏遮挡弹窗问题
- 平板模式导航选中状态异常
- 积分类别需要支持自定义添加
- 整体 UI 交互体验提升

### 2.3 项目命名演进

项目最初称为"积分管理系统"，后经讨论更名为"鼓励储蓄罐"（Reward Jar），更贴切地表达了"储蓄好行为，兑换好奖励"的核心理念。

---

## 三、主要功能模块实现情况

### 3.1 整体架构

```
src/
├── main.tsx              # 应用入口
├── App.tsx               # 根组件（路由、全局布局）
├── index.css             # 全局样式（Tailwind + 自定义动画）
├── components/           # 可复用组件（10个）
│   ├── Header.tsx        # 顶部导航栏（桌面端）
│   ├── BottomNav.tsx     # 底部导航栏（移动端）
│   ├── Toast.tsx         # 全局消息提示
│   ├── ConfirmDialog.tsx # 全局确认对话框
│   ├── ChildCard.tsx     # 孩子信息卡片
│   ├── PointRecordItem.tsx # 积分记录项
│   ├── AddPointModal.tsx # 添加积分弹窗
│   ├── AvatarPicker.tsx  # 头像选择器
│   ├── PinModal.tsx      # 家长密码验证弹窗 [v1.3新增]
│   └── SegmentedControl.tsx # 分段筛选控件
├── pages/                # 页面组件（5个）
│   ├── Home.tsx          # 首页仪表盘
│   ├── Children.tsx      # 孩子管理
│   ├── Records.tsx       # 积分记录
│   ├── RewardShop.tsx    # 奖励商城
│   └── Profile.tsx       # 个人中心（类别管理、家长密码设置）[v1.3新增]
├── store/useStore.ts     # Zustand 全局状态
├── types/index.ts        # TypeScript 类型定义
└── utils/storage.ts      # LocalStorage 工具
```

### 3.2 各模块完成情况

| 模块 | 功能点 | 状态 |
|------|--------|------|
| **首页（Home）** | 统计概览、孩子列表、快捷加分入口、储蓄目标 | ✅ 已完成 |
| **孩子管理（Children）** | 增删改查、头像选择、年龄设置 | ✅ 已完成 |
| **积分记录（Records）** | 按日期分组、类型筛选、孩子筛选 | ✅ 已完成 |
| **奖励商城（RewardShop）** | 奖励列表、积分兑换、自定义奖励 | ✅ 已完成 |
| **个人中心（Profile）** | 类别管理、家长密码设置/修改 [v1.3新增] | ✅ 已完成 |
| **积分类别** | 预设5种获得类别 + 5种消费类别、用户自定义 | ✅ 已完成 |
| **移动端适配** | 响应式布局、底部导航、触屏优化 | ✅ 已完成 |
| **全局交互** | Toast提示、确认对话框、动画过渡 | ✅ 已完成 |
| **数据持久化** | LocalStorage 自动存储与加载 | ✅ 已完成 |
| **家长安全验证** | 6位数字密码、积分操作前验证 [v1.3新增] | ✅ 已完成 |
| **积分规则引擎** | 整数约束、上限1000、余额校验、事务原子性 [v1.3新增] | ✅ 已完成 |
| **输入防护体系** | maxLength限制、正则过滤、类型校验三层防护 [v1.3新增] | ✅ 已完成 |

### 3.3 数据模型

四个核心实体：
- **Child**：孩子信息（姓名、头像、年龄、积分余额）
- **PointRecord**：积分记录（关联孩子、类型、数量、原因、日期）
- **PointCategory**：积分类别（名称、分值、图标、类型）
- **Reward**：奖励物品（名称、所需积分、图标）

### 3.4 预设积分类别

**获得类（earn）**：
| 类别 | 积分 | 图标 |
|------|------|------|
| 小红花奖励 | 2分 | 🌸 |
| 帮助做家务 | 5分 | 🧹 |
| 考试成绩优秀 | 10分 | ⭐ |
| 主动学习 | 8分 | 📚 |
| 照顾弟妹 | 6分 | ❤️ |

**消费类（spend）**：
| 类别 | 积分 | 图标 |
|------|------|------|
| 购买玩具 | 50分 | 🧸 |
| 出去玩 | 30分 | 🎢 |
| 看电视时间 | 10分 | 📺 |
| 买零食 | 5分 | 🍭 |
| 游戏时间 | 15分 | 🎮 |

---

## 四、项目实施过程中遇到的关键问题及解决方案

### 问题一：移动端底部导航遮挡弹窗

| 维度 | 描述 |
|------|------|
| **问题描述** | 底部导航栏（`z-index: 50`）与弹窗（`z-index: 50`）层级冲突，导致弹窗被底部导航遮挡，用户无法操作弹窗内容 |
| **影响范围** | 所有弹窗组件（AddPointModal、ConfirmDialog）在移动端均受影响 |
| **根因分析** | BottomNav 使用了 `z-50` 以确保固定在页面底部，但与弹窗的层级相同，在层叠上下文中导航栏覆盖了弹窗 |
| **解决方案** | 将 `BottomNav.tsx` 的 `z-50` 改为 `z-40`，确保弹窗（`z-50`）始终显示在导航栏上方 |
| **涉及文件** | `src/components/BottomNav.tsx` |

### 问题二：平板模式导航选中状态异常

| 维度 | 描述 |
|------|------|
| **问题描述** | 在平板设备上点击底部导航后，导航项背景变白（异常），点击其他区域后才恢复橙色背景 |
| **影响范围** | iPad 等平板设备上的导航交互体验 |
| **根因分析** | CSS 中 `@media (hover: none)` 规则覆盖了导航项的选中样式，平板设备触发了 `hover` 伪类的特殊行为 |
| **解决方案** | 修改 `index.css` 中 `a:hover` 样式，仅重置 `color` 属性，保留 `background-color` 不覆盖 |
| **涉及文件** | `src/index.css` |

### 问题三：Vite 版本不兼容导致 Cloudflare Pages 构建失败

| 维度 | 描述 |
|------|------|
| **错误信息** | `The version of Vite used in the project ("5.4.21") cannot be automatically configured. Please update the Vite version to at least "6.0.0"` |
| **影响范围** | Cloudflare Pages 部署完全阻塞 |
| **根因分析** | Cloudflare Pages 的构建环境要求 Vite >= 6.0.0，项目使用的是 Vite 5.4.1 |
| **解决方案** | 升级 Vite 至 6.4.3，同步更新 `@vitejs/plugin-react` 至 4.5.2 |
| **涉及文件** | `package.json` |

### 问题四：esbuild 依赖版本冲突

| 维度 | 描述 |
|------|------|
| **错误信息** | `Invalid: lock file's @esbuild/linux-x64@0.21.5 does not satisfy @esbuild/linux-x64@0.25.12`（多个平台 esbuild 版本不匹配） |
| **影响范围** | Cloudflare Pages 部署阻塞，`npm ci` 命令失败 |
| **根因分析** | Windows 环境生成的 `package-lock.json` 锁定了 esbuild 0.21.5（Win32），但 Vite 6.4.3 要求 esbuild 0.25.12。Linux 平台（Cloudflare 构建环境）的依赖版本在 lock 文件中不匹配 |
| **解决方案** | 1. 删除 `package-lock.json` 2. 将其加入 `.gitignore` 3. Cloudflare 构建命令改为 `npm install && npm run build`（用 `npm install` 替代 `npm ci`，让构建环境重新解析依赖） |
| **涉及文件** | `package-lock.json`（已删除）、`.gitignore`、Cloudflare Pages 构建设置 |

### 问题五：_redirects 文件导致无限循环重定向

| 维度 | 描述 |
|------|------|
| **错误信息** | `Invalid _redirects configuration: Line 1: Infinite loop detected in this rule.` |
| **影响范围** | Cloudflare Workers 部署失败 |
| **根因分析** | `public/_redirects` 中配置了 `/* /index.html 200`，Cloudflare Workers 的 wrangler 部署方式检测到此规则会导致无限循环（SPA 路由配置在该环境下不适用） |
| **解决方案** | 删除 `public/_redirects` 文件。对于纯前端静态网站部署，Cloudflare Pages 自动处理 SPA 路由，无需额外配置重定向 |
| **涉及文件** | `public/_redirects`（已删除） |

### 问题六：GitHub Push Protection 拦截敏感信息

| 维度 | 描述 |
|------|------|
| **错误信息** | `Push cannot contain secrets: GitHub Personal Access Token found in .trae/documents/部署教程.md:151` |
| **影响范围** | Git push 被 GitHub 远程拒绝 |
| **根因分析** | 部署教程文档中不慎记录了真实的 GitHub Personal Access Token |
| **解决方案** | 从文档中删除敏感 token，使用 `git commit --amend` 修改提交历史，`git push -f` 强制推送 |
| **涉及文件** | `.trae/documents/部署教程.md` |

### 问题七：微信内置浏览器安全拦截

| 维度 | 描述 |
|------|------|
| **问题描述** | 在微信中打开应用时，提示"网页包含诱导分享、关注等诱导行为内容"，无法正常访问 |
| **影响范围** | 所有通过微信内置浏览器访问的用户 |
| **根因分析** | 新域名 `m.runbt.cn` 尚未被微信安全系统信任，微信 AI 检测机制可能对新配置的域名进行误判。实际页面内容不包含任何诱导分享或关注行为 |
| **解决方案** | 1. 等待域名被微信安全系统自动验证（1-7天） 2. 通过微信安全中心（weixin110.qq.com）提交域名申诉 3. 通过手机原生浏览器正常访问 |

### 问题八：Cloudflare workers.dev 域名国内无法访问

| 维度 | 描述 |
|------|------|
| **问题描述** | `https://mypoint.longw7050.workers.dev` 在中国大陆地区无法直接访问，需要特定网络环境 |
| **影响范围** | 所有国内用户 |
| **根因分析** | Cloudflare 的 `workers.dev` 域名在中国大陆地区受到网络访问限制 |
| **解决方案** | 1. 配置自有域名 `m.runbt.cn` 指向 Cloudflare Workers 2. 将阿里云域名的 DNS 服务器修改为 Cloudflare 3. 通过自有域名正常访问应用 |

### 问题九：文本输入框无长度限制导致布局破坏 [v1.3]

| 维度 | 描述 |
|------|------|
| **问题描述** | 孩子姓名、类别名称等文本输入框无 `maxLength` 限制，输入超长文本后破坏卡片布局；数字输入框可输入超过4位数，导致积分数据异常 |
| **影响范围** | 所有包含输入框的页面（Children、Profile、RewardShop、Home、AddPointModal） |
| **根因分析** | 初始开发时未考虑极端输入场景，缺少前端输入长度限制 |
| **解决方案** | 1. 文本输入框添加 `maxLength`（姓名20、原因50、图标4） 2. 数字输入框限制4位长度 3. 孩子姓名超长时用 `truncate max-w-[120px]` 显示省略号 |
| **涉及文件** | `Children.tsx`、`Profile.tsx`、`RewardShop.tsx`、`Home.tsx`、`AddPointModal.tsx`、`ChildCard.tsx` |

### 问题十：底部导航背景透明导致视觉错乱 [v1.3]

| 维度 | 描述 |
|------|------|
| **问题描述** | 底部导航栏使用半透明渐变背景 `bg-gradient-to-t from-white to-orange-50/20`，滑动页面时可见下层内容，给人排版错乱的感觉 |
| **影响范围** | 移动端所有页面 |
| **根因分析** | 设计初期追求视觉轻盈感，使用了半透明背景，但未考虑到滚动时的视觉穿透问题 |
| **解决方案** | 将底部导航背景改为纯白不透明 `bg-white`，确保滚动时内容完全被遮盖 |
| **涉及文件** | `src/components/BottomNav.tsx` |

### 问题十一：头像选择器存在空位和重复 [v1.3]

| 维度 | 描述 |
|------|------|
| **问题描述** | AvatarPicker 中的头像列表存在空位和重复头像，5x5网格中有空白格 |
| **影响范围** | 添加/编辑孩子时的头像选择体验 |
| **根因分析** | 头像数组元素不足25个，且有重复项 |
| **解决方案** | 补充空位为 🐼、🦄、🍀，确保25个头像无重复、无空位 |
| **涉及文件** | `src/components/AvatarPicker.tsx` |

---

## 五、技术选型与架构设计评估

### 5.1 技术栈选择

| 类别 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| 框架 | React | 18.3.1 | 生态成熟、组件化开发效率高 |
| 语言 | TypeScript | 5.5.3 | 类型安全，减少运行时错误 |
| 构建 | Vite | 6.4.3 | 极速开发体验、高效生产构建 |
| 路由 | React Router | 6.26.2 | SPA 路由标准方案 |
| 状态管理 | Zustand | 4.5.5 | 轻量、API简洁、无 boilerplate |
| 样式 | Tailwind CSS | 3.4.11 | 响应式设计快、开发效率高 |
| 图标 | Lucide React | 0.447.0 | 一致的图标风格、Tree-shaking 友好 |
| 数据存储 | LocalStorage | - | 零成本、免服务器、数据隐私 |

### 5.2 架构设计评估

**优点**：
- **无后端依赖**：纯前端应用，部署简单，零服务器成本
- **状态管理清晰**：Zustand + LocalStorage 的架构简洁高效，数据流单向透明
- **组件复用良好**：Toast、ConfirmDialog、ChildCard、PinModal 等组件在多页面复用
- **响应式设计完整**：覆盖手机（xs）、平板（md）、桌面（lg）全断点
- **安全验证机制** [v1.3]：6位数字家长密码保护积分操作，防止孩子误操作
- **输入防护完善** [v1.3]：maxLength + 正则过滤 + 类型校验三层防护，暴力测试13项全部通过

**待改进**：
- **数据安全性**：LocalStorage 数据无备份机制，浏览器清除数据会导致数据丢失
- **多设备同步**：目前不支持跨设备数据同步
- **密码存储**：家长密码以明文存储在 LocalStorage，存在被查看的风险

### 5.3 部署方案评估

| 方案 | 部署地址 | 国内可访问 | 免费额度 | 状态 |
|------|----------|------------|----------|------|
| Cloudflare Workers + 自定义域名 | `m.runbt.cn` | ✅ 正常 | 无限带宽 | 已上线 |
| Cloudflare Workers 默认域名 | `mypoint.longw7050.workers.dev` | ❌ 受限 | 无限带宽 | 备用 |

---

## 六、项目成果与价值评估

### 6.1 核心成果

| 指标 | 数据 |
|------|------|
| 项目开发周期 | 约 2 周（v1.0 → v1.3） |
| 核心页面 | 5 个（首页、孩子管理、积分记录、奖励商城、个人中心） |
| 可复用组件 | 10 个 |
| 代码行数 | 约 2000+ 行（TypeScript + TSX + CSS） |
| 预设积分类别 | 10 种（5种获得 + 5种消费） |
| 安全机制 | 6位家长密码验证 + 三层输入防护 |
| 暴力测试 | 13项测试全部通过 |
| 支持平台 | iOS Safari、Android Chrome、桌面浏览器、微信内置浏览器 |
| 部署方式 | Cloudflare Workers + 自定义域名 |
| 技术栈 | React 18 + TypeScript + Tailwind CSS + Zustand + Vite |

### 6.2 价值评估

1. **家庭教育价值**：为家长提供了一套完整的、可操作的孩子行为激励机制
2. **技术价值**：验证了纯前端应用 + 免费云部署的可行性，零成本上线
3. **学习价值**：完整实践了 React + TypeScript + Tailwind CSS 的技术栈，涵盖状态管理、响应式设计、部署运维等全流程

### 6.3 版本迭代记录

| 日期 | 版本 | 更新内容 |
|------|------|---------|
| 2026-05-25 | v1.0 | 初始版本，完成核心功能（孩子管理、积分记录、奖励商城） |
| 2026-06-06 | v1.1 | 移动端UI优化、Toast/ConfirmDialog组件、自定义积分类别 |
| 2026-06-06 | v1.2 | 项目更名为"鼓励储蓄罐"、完善中英文文档、修复部署问题 |
| 2026-06-07 | v1.3 | 家长安全验证、积分规则引擎、输入防护、暴力测试、样式规范定调 |

---

## 七、经验教训

### 7.1 做得好的地方

1. **技术选型务实**：选择了 Zustand 而非 Redux，选择了 Tailwind CSS 而非独立 CSS 方案，降低了复杂度
2. **移动端优先**：从一开始就以移动端为主要使用场景进行设计，避免了后期大量改造
3. **组件化设计**：Toast、ConfirmDialog、PinModal 等全局组件从页面中抽离，提高了代码复用性
4. **数据持久化自动化**：每次状态变更自动写入 LocalStorage，用户无需手动保存
5. **暴力测试先行** [v1.3]：通过模拟熊孩子极端操作的自动化测试，提前发现并修复了输入验证、布局破坏等隐患
6. **设计规范沉淀** [v1.3]：在开发过程中同步整理样式定调文档，确保后续开发风格一致

### 7.2 需要改进的地方

1. **依赖版本管理**：未在项目初期锁定 `package-lock.json` 的跨平台兼容性，导致部署时出现 esbuild 版本冲突
2. **部署文档安全**：文档中不慎包含了敏感信息（GitHub Token），触发了 GitHub 的安全拦截
3. ~~**缺少自动化测试**：项目没有任何单元测试或 E2E 测试，依赖人工验证~~ [v1.3已部分解决：暴力测试脚本已建立]
4. **错误边界缺失**：React 应用未添加 Error Boundary，组件崩溃会导致整个页面白屏
5. **域名准备工作不充分**：未提前准备已备案的自有域名，临时配置导致延迟
6. **输入验证滞后** [v1.3]：初始开发时未同步加入输入限制，导致后期集中修补。应在开发每个输入框时就同步添加验证

### 7.3 关键教训

1. **跨平台构建环境差异**：`package-lock.json` 在不同操作系统间可能存在不兼容，需要在 CI/CD 环境中生成或直接忽略 lock 文件
2. **安全编码意识**：文档中绝不能包含真实的 Token、密码等敏感信息
3. **网络环境适配**：面向国内用户的应用，需优先考虑国内可访问的部署方案和域名
4. **输入验证必须前置** [v1.3]：每个输入组件开发时就应同步加入 `maxLength`、类型校验和边界值限制，而非事后修补
5. **暴力测试很有价值** [v1.3]：面向儿童/非专业用户的应用，必须模拟极端操作场景进行测试，常规功能测试无法覆盖

---

## 八、未来改进方向

### 8.1 短期优化（v1.4）

| 优先级 | 改进项 | 描述 |
|--------|--------|------|
| 高 | 数据导出功能 | 支持将积分记录导出为 CSV/Excel 文件 |
| 高 | 数据备份提醒 | 定期提醒用户备份 LocalStorage 数据 |
| 高 | 密码加密存储 | 家长密码使用哈希存储，避免明文泄露 |
| 中 | 错误边界 | 添加 React Error Boundary 防止白屏 |
| 中 | PWA 支持 | 添加 Service Worker，支持离线访问和添加到主屏幕 |
| 低 | 暗色模式 | 支持深色/浅色主题切换 |

### 8.2 中期规划（v2.0）

| 改进项 | 描述 |
|--------|------|
| 云数据同步 | 集成 LeanCloud 或 Supabase，实现跨设备数据同步 |
| 用户认证 | 添加家庭成员认证，区分操作者身份 |
| 数据统计 | 增加积分趋势图表、周/月度报表 |
| 通知提醒 | 支持积分变动推送通知 |

### 8.3 长期规划（v3.0）

| 改进项 | 描述 |
|--------|------|
| 后端服务化 | 自建后端 API，支持更多高级功能 |
| 多家庭支持 | 支持多个家庭独立管理各自的积分系统 |
| AI 行为建议 | 基于积分记录，提供个性化育儿建议 |

---

## 九、附录

### 9.1 项目信息

| 属性 | 值 |
|------|-----|
| 项目名称（中文） | 鼓励储蓄罐 |
| 项目名称（英文） | Reward Jar |
| GitHub 仓库 | `https://github.com/longw7050-prog/mypoint` |
| 在线地址 | `https://m.runbt.cn` |
| 技术栈 | React 18 + TypeScript + Tailwind CSS + Zustand + Vite |

### 9.2 文件清单

| 文件 | 用途 |
|------|------|
| `src/types/index.ts` | TypeScript 类型定义 |
| `src/utils/storage.ts` | LocalStorage 操作工具 |
| `src/store/useStore.ts` | 全局状态管理 |
| `src/components/AddPointModal.tsx` | 积分添加/消费弹窗 |
| `src/components/AvatarPicker.tsx` | 头像选择器 |
| `src/components/BottomNav.tsx` | 移动端底部导航 |
| `src/components/ChildCard.tsx` | 孩子信息卡片 |
| `src/components/ConfirmDialog.tsx` | 确认对话框 |
| `src/components/Header.tsx` | 桌面端顶部导航 |
| `src/components/PointRecordItem.tsx` | 积分记录项 |
| `src/components/PinModal.tsx` | 家长密码验证弹窗 [v1.3新增] |
| `src/components/SegmentedControl.tsx` | 分段筛选控件 |
| `src/components/Toast.tsx` | 消息提示 |
| `src/pages/Home.tsx` | 首页仪表盘 |
| `src/pages/Children.tsx` | 孩子管理页 |
| `src/pages/Records.tsx` | 积分记录页 |
| `src/pages/RewardShop.tsx` | 奖励商城页 |
| `src/pages/Profile.tsx` | 个人中心页 [v1.3新增] |
| `src/App.tsx` | 根组件 |
| `src/main.tsx` | 应用入口 |
| `.trae/documents/积分管理系统实现方案.md` | 技术方案文档 |
| `.trae/documents/部署教程.md` | 部署教程文档 |
| `.trae/documents/项目阶段性总结.md` | 本文档 |
| `.trae/documents/2026-06-07.md` | v1.3 每日修改详细记录 |

### 9.3 项目时间线

```
2026-05-25  v1.0  完成核心功能开发
2026-06-06  v1.1  移动端优化、组件完善、积分类别自定义
2026-06-06  v1.2  更名、文档完善、部署配置修复
2026-06-07  v1.3  家长安全验证、积分规则引擎、输入防护、暴力测试、样式规范
```

---

> **文档维护**：本文档随项目迭代持续更新，最新版本见 `.trae/documents/项目阶段性总结.md`

---

## 十、样式设计规范 [v1.3]

> 以下为项目当前确定的视觉风格与设计规范，后续开发请保持一致。详细版本见 `.trae/documents/2026-06-07.md`。

### 10.1 色彩体系

| 角色 | 色名 | 色值 | 用途 |
|------|------|------|------|
| 主色 | 活力橙 | `#FF9F43` | 品牌色、主按钮、积分高亮、选中态 |
| 辅色 | 天空蓝 | `#54A0FF` | 辅助信息、链接、次要操作 |
| 强调色 | 珊瑚红 | `#FF6B6B` | 积分扣除、警告、重要提示 |
| 成功色 | 翡翠绿 | `#10B981` | 积分获得、成功提示、正向反馈 |
| 警告色 | 琥珀黄 | `#F59E0B` | 警告提示、待处理状态 |
| 危险色 | 警示红 | `#EF4444` | 删除操作、危险确认、错误提示 |

**渐变用法**：
- 主色渐变：`from-primary to-accent`（橙→红，头像背景、积分数字）
- 页面背景：`from-orange-50/50 via-white to-gray-50`（暖→白→灰）
- 卡片背景：`from-white to-orange-50/30`（白→微橙）
- 积分区域：`from-primary/10 to-accent/10`（淡橙→淡红）
- 获得统计：`from-green-50 to-emerald-50/50`
- 消费统计：`from-orange-50 to-amber-50/50`

### 10.2 圆角规范

| 元素 | Tailwind 类 |
|------|-------------|
| 页面区块/卡片 | `rounded-2xl` |
| 弹窗容器 | `rounded-2xl`（PC）/ `rounded-t-2xl`（移动端底部弹出） |
| 按钮（主操作） | `rounded-xl` |
| 输入框 | `rounded-lg` |
| 头像/图标容器 | `rounded-xl` |

### 10.3 阴影规范

| 层级 | Tailwind 类 | 用途 |
|------|-------------|------|
| 轻阴影 | `shadow-sm` | 卡片、区块容器 |
| 中阴影 | `shadow-md` | 悬停态、选中态 |
| 重阴影 | `shadow-2xl` | 弹窗 |

### 10.4 字体与字号

- **字体族**：`Inter, system-ui, sans-serif`

| 场景 | Tailwind 类 |
|------|-------------|
| 页面标题 | `text-lg` / `text-xl` |
| 区块标题 | `text-sm font-semibold` / `text-base font-semibold` |
| 正文/标签 | `text-xs` / `text-sm` |
| 积分数字 | `text-lg font-bold` / `text-2xl font-bold` |
| 统计数字 | `text-base font-bold` |

### 10.5 间距与布局

- **页面内边距**：`px-4 pt-4 pb-4`
- **卡片内边距**：`p-4`
- **网格布局**：`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3`
- **最大宽度**：`max-w-6xl`

### 10.6 交互微动效

| 交互 | Tailwind 类 |
|------|-------------|
| 按钮按下 | `active:scale-95` |
| 按钮悬停 | `hover:shadow-md` |
| 弹窗出现 | `animate-scaleIn`（PC）/ `animate-slideUp`（移动端） |
| 弹窗背景 | `animate-fadeIn` |
| 列表展开 | `animate-slideDown` |
| 颜色过渡 | `transition-colors` / `transition-all` |

### 10.7 组件风格约定

- **导航栏**：PC 端顶部 Header + 移动端底部 BottomNav，背景纯白不透明
- **弹窗**：半透明黑色遮罩 `bg-black/50`，白色内容区，sticky 顶栏带关闭按钮
- **表单输入**：`bg-gray-50 border-0`，聚焦时 `ring-1 ring-primary`
- **主按钮**：`bg-gradient-to-r from-primary to-primary/80 text-white`
- **次按钮**：`bg-gray-50 text-gray-600 hover:bg-gray-100`
- **危险按钮**：`bg-red-50 text-red-500 hover:bg-red-100`
- **积分数字**：`bg-gradient-to-r from-primary to-accent bg-clip-text text-transparent`
- **超长文本**：`truncate max-w-[120px]`，防止破坏布局

### 10.8 响应式断点

| 断点 | 宽度 | 布局变化 |
|------|------|---------|
| 移动端 | <640px | 单列、底部导航、弹窗底部弹出 |
| 平板 | 640-1024px | 双列、顶部导航 |
| 桌面 | >1024px | 三列、顶部导航 |
